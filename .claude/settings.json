{
  "permissions": {
    "allow": [
      "Bash(git commit -m \"fix: only run search query when filters are active\")",
      "Bash(git add packages/db/prisma/schema/)",
      "Bash(git commit -m \"feat: add performance indexes to Prisma schema models\")",
      "Bash(bun prisma migrate dev --name add_performance_indexes --create-only)"
    ]
  }
}
