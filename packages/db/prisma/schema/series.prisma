///////////////////////////
// SERIES
///////////////////////////

model Series {
    id          String  @id @default(cuid())
    title       String
    slug        String  @unique
    description String?
    coverImage  String?

    posts Post[]

    createdAt DateTime @default(now())
    updatedAt DateTime @updatedAt
}
