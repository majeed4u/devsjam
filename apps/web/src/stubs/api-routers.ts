// Stub type for the frontend oRPC client
// In production, this type is inferred from the real backend router
// In the sandbox, we provide a compatible stub so Vite can start
export type AppRouter = any;
export type AppRouterClient = any;
export const appRouter = {} as any;
